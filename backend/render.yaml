services:
  - type: web
    name: socialsieve-backend
    env: python
    rootDir: backend
    buildCommand: ./render-build.sh
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    pythonVersion: 3.10
    autoDeploy: true
